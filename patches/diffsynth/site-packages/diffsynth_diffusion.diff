diff --git a/diffsynth/diffusion/logger.py b/diffsynth/diffusion/logger.py
index 7c666ba..9dd6a30 100644
--- a/diffsynth/diffusion/logger.py
+++ b/diffsynth/diffusion/logger.py
@@ -56,7 +56,11 @@ class ModelLogger:
         self.output_path = output_path
         self.remove_prefix_in_ckpt = remove_prefix_in_ckpt
         self.state_dict_converter = state_dict_converter
-        self.num_steps = 0
+        # Cumulative-step base so a warm-started run continues the checkpoint
+        # lineage (step-700, step-800, ...) instead of restarting at step-100 and
+        # clobbering the previous run's files. Adapter weights carry over on a
+        # warm start; optimizer/scheduler state does not (DiffSynth saves neither).
+        self.num_steps = int(os.environ.get("DIFFSYNTH_STEP_OFFSET") or 0)
         # Loggers
         self.enable_tensorboard_log = enable_tensorboard_log
         self.enable_swanlab_log = enable_swanlab_log
diff --git a/diffsynth/diffusion/runner.py b/diffsynth/diffusion/runner.py
index 0e2c921..02175c0 100644
--- a/diffsynth/diffusion/runner.py
+++ b/diffsynth/diffusion/runner.py
@@ -110,25 +110,39 @@ def launch_data_process_task(
         enable_optimizer_cpu_offload = args.enable_optimizer_cpu_offload
         cpu_offload_split_threshold = args.cpu_offload_split_threshold
         
-    dataloader = torch.utils.data.DataLoader(dataset, shuffle=False, collate_fn=lambda x: x[0], num_workers=num_workers)
+    # Index-first loop so we can skip already-cached samples without reloading media.
     if enable_model_cpu_offload:
-        dataloader = accelerator.prepare(dataloader)
         offload_manager = OffloadTrainingManager(model, accelerator.device, enable_optimizer_cpu_offload, cpu_offload_split_threshold)
         model.pipe.device = accelerator.device
+        model = accelerator.prepare(model)
     else:
         model.to(device=accelerator.device)
-        model, dataloader = accelerator.prepare(model, dataloader)
-    
-    for data_id, data in enumerate(tqdm(dataloader)):
-        with accelerator.accumulate(model):
+        model = accelerator.prepare(model)
+
+    folder = os.path.join(model_logger.output_path, str(accelerator.process_index))
+    os.makedirs(folder, exist_ok=True)
+    skipped = 0
+    processed = 0
+    failed = 0
+    for data_id in tqdm(range(len(dataset))):
+        save_path = os.path.join(folder, f"{data_id}.pth")
+        if os.path.exists(save_path):
+            skipped += 1
+            continue
+        try:
+            data = dataset[data_id]
             with torch.no_grad():
-                folder = os.path.join(model_logger.output_path, str(accelerator.process_index))
-                os.makedirs(folder, exist_ok=True)
-                save_path = os.path.join(model_logger.output_path, str(accelerator.process_index), f"{data_id}.pth")
                 data = model(data)
                 torch.save(data, save_path)
-                if enable_model_cpu_offload:
-                    offload_manager.after_backward()
+            processed += 1
+            if enable_model_cpu_offload:
+                offload_manager.after_backward()
+        except Exception as e:
+            failed += 1
+            print(f"[data_process] SKIP data_id={data_id} due to {type(e).__name__}: {e}")
+            continue
+    print(f"[data_process] done skipped_existing={skipped} processed={processed} failed={failed} total={len(dataset)}")
+
 
 def initialize_deepspeed_gradient_checkpointing(accelerator: Accelerator):
     if getattr(accelerator.state, "deepspeed_plugin", None) is not None:
diff --git a/diffsynth/diffusion/training_module.py b/diffsynth/diffusion/training_module.py
index a3c6143..e46146d 100644
--- a/diffsynth/diffusion/training_module.py
+++ b/diffsynth/diffusion/training_module.py
@@ -183,9 +183,12 @@ class DiffusionTrainingModule(torch.nn.Module):
         if model_paths is not None:
             model_paths = json.loads(model_paths)
             for path in model_paths:
+                # A sharded model is a single entry holding a list of shard paths, so naming any
+                # one shard in `--fp8_models` / `--offload_models` selects the whole group.
+                shards = path if isinstance(path, list) else [path]
                 vram_config = self.parse_vram_config(
-                    fp8=path in fp8_models,
-                    offload=path in offload_models,
+                    fp8=any(shard in fp8_models for shard in shards),
+                    offload=any(shard in offload_models for shard in shards),
                     device=device
                 )
                 model_configs.append(ModelConfig(path=path, **vram_config))
